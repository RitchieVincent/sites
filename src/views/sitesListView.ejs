<% layout('layout') -%>

<div class="title">  
    <%= title %>
</div>
        
<ul>

    <% for(var i=0; i<sites.length;i++){ %>

        <li>
            <%= sites[i].title %> - 
            <%= sites[i].developer %>
            <a href="/sites/<%= sites[i]._id %>">Read More</a>
        </li>

    <% } %>

</ul>